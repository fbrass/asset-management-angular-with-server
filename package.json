{
  "name": "selenium-testing-server",
  "private": true,
  "author":"fbrass",
  "version": "0.0.0",
  "description": "Selenium testing purposes only",
  "license": "MIT",
  "dependencies": {
    "express": "^4.11.2",
    "connect":"^2.27.3"
  },
  "devDependencies": {
    "bower": "^1.3.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",
    "pretest": "npm install"
  }
}